@page
@model GadgetHub.Pages.Sales.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sales[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sales[0].Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sales[0].Products)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Sales) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerFirstname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Products.ProductName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SalesID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SalesID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SalesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
